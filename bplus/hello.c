#include <stdio.h>
#include < stdbool.h>

struct BTree {
	struct Node* root;
	int t;
};

struct Node {
	bool isLeaf;
	int* keys;
	struct Node** C;
	int lenKey;
	int t;
};

void insertKey(struct Node* targetNode, int key) {
	keys = targetNode->keys;
	count = 0;
	for(int i=0; i<)
	return;
}

int main() {

	return 0;
}